# Applies to builds with context = repo root. (leaderboard-service now builds
# with its own app-dir context + leaderboard-service/.dockerignore.)
# Keeps host-side artifacts out of the image.
.git
.tmp
**/node_modules
**/.venv
**/.next
**/out
**/__pycache__
