FROM python:3.12-slim
ENV PYTHONUNBUFFERED=1
ENV PYTHONIOENCODING=UTF-8
WORKDIR /app
COPY --from=git /project .
RUN pip install -r telemetry-dashboard/requirements.txt
EXPOSE 8000
ENTRYPOINT ["uvicorn", "telemetry-dashboard.main:api", "--host", "0.0.0.0", "--port", "8000"]
