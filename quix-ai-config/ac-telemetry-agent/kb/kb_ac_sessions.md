# AC Telemetry Sessions Index

Every partition combination currently available in the `ac_telemetry` QuixLake table. One section per session; fields inside each section are self-contained so semantic retrieval can surface a single session without losing column alignment.

_Generated: 2026-05-31 16:48 UTC. Sessions: 45._

## Sessions

Each session is described in a short prose paragraph. The values shown are the literal partition values — use them verbatim in SQL `WHERE` clauses or plot trace JSON.

### Session 2026-04-15T10:02:25.041Z

Driver `daniel` ran experiment `BridgeConfigTest` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-15T10:02:25.041Z'` together with `driver = 'daniel'`, `experiment = 'BridgeConfigTest'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-15T09:13:37.600Z

Driver `daniel` ran experiment `ConfigBridgeTest` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-15T09:13:37.600Z'` together with `driver = 'daniel'`, `experiment = 'ConfigBridgeTest'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:00:00.000Z

Driver `alice` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:00:00.000Z'` together with `driver = 'alice'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:40:00.008Z

Driver `elena` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:40:00.008Z'` together with `driver = 'elena'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:20:00.004Z

Driver `emma` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:20:00.004Z'` together with `driver = 'emma'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:10:00.002Z

Driver `john` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:10:00.002Z'` together with `driver = 'john'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:25:00.005Z

Driver `liam` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:25:00.005Z'` together with `driver = 'liam'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:45:00.009Z

Driver `lucas` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:45:00.009Z'` together with `driver = 'lucas'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:35:00.007Z

Driver `marco` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:35:00.007Z'` together with `driver = 'marco'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:15:00.003Z

Driver `maria` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:15:00.003Z'` together with `driver = 'maria'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:05:00.001Z

Driver `peter` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:05:00.001Z'` together with `driver = 'peter'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-28T10:30:00.006Z

Driver `sofia` ran experiment `LeaderBoard` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-05-28T10:30:00.006Z'` together with `driver = 'sofia'`, `experiment = 'LeaderBoard'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T14:56:28.037Z

Driver `ludvik` ran experiment `VideoStartSeek` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2. To reference this session, use `session_id = '2026-04-14T14:56:28.037Z'` together with `driver = 'ludvik'`, `experiment = 'VideoStartSeek'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-15T10:02:25.041Z

Driver `ludvik` ran experiment `VideoStartSeek` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-15T10:02:25.041Z'` together with `driver = 'ludvik'`, `experiment = 'VideoStartSeek'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T11:42:08.107Z

Driver `ludvik` ran experiment `video_streaming` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-04-14T11:42:08.107Z'` together with `driver = 'ludvik'`, `experiment = 'video_streaming'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T11:42:08.107Z

Driver `ludvik` ran experiment `video_streaming` on track `NA` in car `NA` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-14T11:42:08.107Z'` together with `driver = 'ludvik'`, `experiment = 'video_streaming'`, `track = 'NA'`, `carModel = 'NA'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-17T06:35:49.976Z

Driver `ludvik` ran experiment `VideoSyncFix` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-17T06:35:49.976Z'` together with `driver = 'ludvik'`, `experiment = 'VideoSyncFix'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-17T06:39:45.652Z

Driver `ludvik` ran experiment `VideoSyncFix` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3, 4. To reference this session, use `session_id = '2026-04-17T06:39:45.652Z'` together with `driver = 'ludvik'`, `experiment = 'VideoSyncFix'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T13:59:55.013Z

Driver `ludvik` ran experiment `VideoSyncLaps` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3. To reference this session, use `session_id = '2026-04-14T13:59:55.013Z'` together with `driver = 'ludvik'`, `experiment = 'VideoSyncLaps'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T14:06:59.113Z

Driver `ludvik` ran experiment `VideoSyncLaps` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1. To reference this session, use `session_id = '2026-04-14T14:06:59.113Z'` together with `driver = 'ludvik'`, `experiment = 'VideoSyncLaps'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-04-14T14:06:59.113Z

Driver `tomas` ran experiment `VideoSyncLaps` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `g29`). Recorded laps: 1, 2, 3, 4, 5, 6. To reference this session, use `session_id = '2026-04-14T14:06:59.113Z'` together with `driver = 'tomas'`, `experiment = 'VideoSyncLaps'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'g29'`.

### Session 2026-05-25T00:00:00Z

Driver `amateur_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'amateur_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `expert_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'expert_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `learner_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'learner_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `novice_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'novice_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `pro_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'pro_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `tomas_sim` ran experiment `LeaderboardSeed_sim__layout_gp_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'tomas_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `amateur_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'amateur_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `expert_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'expert_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `learner_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'learner_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `novice_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'novice_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `pro_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'pro_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `tomas_sim` ran experiment `LeaderboardSeed_sim__layout_gp_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'tomas_sim'`, `experiment = 'LeaderboardSeed_sim__layout_gp_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `amateur_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'amateur_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `expert_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'expert_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `learner_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'learner_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `novice_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'novice_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `pro_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'pro_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `tomas_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_a` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'tomas_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_a'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `amateur_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'amateur_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `expert_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'expert_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `learner_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'learner_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `novice_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'novice_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `pro_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'pro_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.

### Session 2026-05-25T00:00:00Z

Driver `tomas_sim` ran experiment `LeaderboardSeed_sim__layout_sprint_b` on track `ks_nurburgring` in car `bmw_1m` (environment `prague_office`, test_rig `sim`). Recorded laps: 1. To reference this session, use `session_id = '2026-05-25T00:00:00Z'` together with `driver = 'tomas_sim'`, `experiment = 'LeaderboardSeed_sim__layout_sprint_b'`, `track = 'ks_nurburgring'`, `carModel = 'bmw_1m'`, `environment = 'prague_office'`, `test_rig = 'sim'`.
