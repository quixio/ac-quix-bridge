.env
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
compose.local.yaml
.secrets
certificates/
.quix.yaml.variables
ac-machine-secrets/
recordings/
.DS_Store
.claude/settings.local.json

# Reference repo (test manager source)
quixers-testmanager-standalone/
test-manager-backend/scripts/
telemetry-comparison/scripts/
dcm_source_code/

# Playwright MCP artifacts (smoke tests, agent screenshots, MCP cache)
.playwright-mcp/
*.png

# Mock server for local UI testing
telemetry-comparison/mock/

# Local Claude Code settings
.claude/settings.local.json

# Node tooling (prettier + eslint for telemetry-comparison/static)
node_modules/

# Planning / scratch docs
dev-planning/
docs/

# Local Docker build-context overrides (Windows symlink workaround etc.)
.dockerignore

# scratch
.tmp/

docker-compose.dev.yml
