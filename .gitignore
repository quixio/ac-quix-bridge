.env
env/.env*

# quix-ai-config writes script-state to its own per-env .env files
quix-ai-config/.env
quix-ai-config/.env.*
!quix-ai-config/.env.example
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
compose.local.yaml
.secrets
certificates/
*.pem
.quix.yaml.variables
ac-machine-secrets/
recordings/
logs/
*.log
.DS_Store
.claude/settings.local.json

# Reference repo (test manager source)
quixers-testmanager-standalone/
test-manager-backend/scripts/
telemetry-comparison/scripts/
dcm_source_code/

# Playwright MCP artifacts (smoke tests, agent screenshots, MCP cache)
.playwright-mcp/
*.png

# Mock server for local UI testing
telemetry-comparison/mock/

# Local Claude Code settings
.claude/settings.local.json

# Node tooling (prettier + eslint for telemetry-comparison/static)
node_modules/

# Planning / scratch docs
dev-planning/

# scratch
.tmp/

# Local-only architecture notes (docs/ is otherwise tracked)
docs/architecture-leaderboard-*.md

# Local backend Docker artifacts
test-manager-backend/.dockerignore
